@model COD.Models.ViewModels.CategorySubCategoryViewModel
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Model.Catgory.Name
        </th>
        <th></th>
    </tr>

@foreach (var item in Model.SubCategories) {
<tr>
    <td>@item.Name</td>
    <td>@item.Id</td>
</tr>
}

</table>
